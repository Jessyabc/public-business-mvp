/**
 * UI Feature Flags
 * 
 * Toggles for experimental features and legacy code paths.
 */

/**
 * Enable legacy CSS imports
 * 
 * Set to true to import legacy stylesheets (base.css, glass.css, etc.)
 * These are slated for removal once all components migrate to the new
 * theme system and effects.css module.
 * 
 * @default false
 */
export const ENABLE_LEGACY_STYLES = false;
